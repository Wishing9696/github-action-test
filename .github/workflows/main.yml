name: CI

on: 
    push:
        branches:
            - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
          uses: actions/checkout@v2
        
        # - name: Build Docker image
        #   run: |
        #     docker build -t go-github-action-test-docker-image .

        # - name: Login to DockerHub
        #   uses: docker/login-action@v1
        #   with:
        #     username: ${{ secrets.DOCKERHUB_USERNAME }}
        #     password: ${{ secrets.DOCKERHUB_TOKEN }}

        # - name: Push Docker image
        #   run: |
        #     docker push username/go-github-action-test-docker-image:latest
        #     docker push username/go-github-action-test-docker-image:${{ github.sha }}